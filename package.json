{
  "name": "dsh-desktop",
  "version": "0.2.11",
  "private": true,
  "description": "DeepSeek Harness desktop app — Tauri 2 shell + bundled Node runtime + pruned dsh dependencies",
  "type": "module",
  "scripts": {
    "fetch:node": "node scripts/fetch-node.mjs",
    "fetch:node:universal": "node scripts/fetch-node.mjs --universal",
    "pack:runtime": "node scripts/pack.mjs",
    "pack:runtime:min": "node scripts/pack.mjs --minify",
    "pack:runtime:universal": "node scripts/pack.mjs --minify --universal",
    "test:compat": "node --test scripts/legacy-webkit-compat.test.mjs",
    "prepare:runtime": "npm run fetch:node && npm run pack:runtime:min",
    "prepare:runtime:universal": "npm run fetch:node:universal && npm run pack:runtime:universal",
    "tauri": "tauri",
    "build": "npm run prepare:runtime && tauri build",
    "bundle": "npm run prepare:runtime && tauri build",
    "bundle:universal": "npm run prepare:runtime:universal && tauri build --target universal-apple-darwin",
    "dev": "npm run pack:runtime && tauri dev"
  },
  "dependencies": {
    "@deepseek-ai/dsh": "^0.1.0-rc.6",
    "@ungap/structured-clone": "^1.3.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "esbuild": "^0.24.0"
  }
}
